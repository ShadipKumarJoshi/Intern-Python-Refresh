# Write a program that uses the random module to generate a random number.

import random

random_num = random.randint(0,100)  # random number in range 0-100

print(" A random number in 0-100 is:", random_num)